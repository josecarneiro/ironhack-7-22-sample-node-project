<nav class="navbar navbar-expand-lg bg-light mb-3">
  <div class="container">
    <a class="navbar-brand" href="/">Home</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        {{#if user}}
        <li class="nav-item">
          <a class="nav-link" href="/profile/{{user._id}}">{{user.name}}'s Profile</a>
        </li>
        <form action="/authentication/sign-out" method="POST">
          <button class="btn nav-link">Sign Out</button>
        </form>
        {{else}}
        <li class="nav-item">
          <a class="nav-link" href="/authentication/sign-in">Sign In</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/authentication/sign-up">Sign Up</a>
        </li>
        {{/if}}

      </ul>
    </div>
  </div>
</nav>
